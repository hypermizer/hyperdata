{
  "name": "hyperdata",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A free, GitHub-hosted Hyperliquid market dashboard and price alert monitor.",
  "scripts": {
    "test": "node --test",
    "check": "node --check public/app.js && node --check public/lib/alerts.js && node --check public/lib/hyperliquid.js && node --check public/lib/supabase.js && node --check scripts/check-alerts.js",
    "alerts": "node scripts/check-alerts.js",
    "serve": "npx --yes serve public"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "nodemailer": "^9.0.3"
  }
}
